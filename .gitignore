# -------------------------------------------------------------------
# КРИТИЧЕСКИ ВАЖНО: Секреты и Переменные Окружения
# НИКОГДА не коммитьте эти файлы, так как они содержат токены, ключи и пароли!
# -------------------------------------------------------------------
.env
.env.local
.env.*.local

# -------------------------------------------------------------------
# ЛОКАЛЬНЫЕ ДАННЫЕ И КЭШ
# -------------------------------------------------------------------
# Зависимости
/node_modules
.npm
.yarn-cache

# Логи
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Артефакты сборки (Vite/React/Express)
/dist
/build
/dist-ssr
/out

# -------------------------------------------------------------------
# БАЗЫ ДАННЫХ И СЕРВИСЫ ТУННЕЛЕЙ (Prisma, Ngrok/Localtunnel)
# -------------------------------------------------------------------
# Локальные базы данных (Prisma использует эти имена по умолчанию)
/prisma/dev.db 
/prisma/*.sqlite
*.sqlite 

# Файлы туннелей (чтобы случайно не закоммитить их кэш/логи)
*.ngrok.*
*.loca.lt

# -------------------------------------------------------------------
# ФАЙЛЫ ОС И РЕДАКТОРОВ (VS Code, WebStorm, Mac OS)
# -------------------------------------------------------------------
# Файлы операционных систем
.DS_Store
Thumbs.db

# VSCode
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json # Иногда полезно закоммитить, если нужно унифицировать настройки

# IDE (JetBrains - WebStorm, IDEA)
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
